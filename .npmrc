# Obsidian expects release tags to be the bare version, with no "v" prefix, matching
# manifest.json. Without this, `npm version` would tag v0.2.0 and release.yml would not fire.
tag-version-prefix=""

# Pin exact versions: package-lock.json and package.json must agree on what shipped.
save-exact=true
